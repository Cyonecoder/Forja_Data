version: "3.9"

services:
  forjaagent:
    build: .
    container_name: forjaagent
    command: uvicorn app:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./:/app
    working_dir: /app